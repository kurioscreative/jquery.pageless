!!! 5
%head
  %title Pageless Example
  = stylesheet_link_tag :all
  = javascript_include_tag :defaults
  = javascript_include_tag 'jquery.pageless.min.js'
  = csrf_meta_tag
%body{ :lang => 'en', :class => "#{controller.controller_name}" }
  #container
    %header
      %h1 Pageless (A Rails 3.0 Example)
    #content
      = render :partial => 'layouts/flashes'
      = yield
    %footer
      %p If you're seeing this, one of a couple things is happening: a) you're at the bottom of 100 articles, b) javascript isn't enabled, or c) something is going wrong.